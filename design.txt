The core objectives of cTrader openAPI python is to migrate as close as possible to mq4 code base and cBot code base in folder
./code_references/cBot and ./code_references/mq4

the mq4 is the original code base from where the functionality was migrated to cBot.

Now the current object is to migrate to cTrader open api.

the mq4 has a lot of code bloat and I do not need all of it. I only need to migrate its current code execution path.

- it's execution path begins inside void   HSIG::initSIG(const SANSIGNALS &ss, SanUtils &util)

TASK 1:


wrt to code_base_v98.txt: Migrate the following:

SAN_SIGNAL SanSignals::kineticAccelerationSIG(
   const double fastSlope,      // e.g., 3-period slope
   const double slowSlope,      // e.g., 10-period slope
   const double tradeZoneCheck, // do not trade if slow slope is flat-lining
   const double tradeCloseLimit,   // The ratio limit which closes this trade call
   string funcLabel    // Which function makes this call
)

SAN_SIGNAL HSIG::fuseSIG(SAN_SIGNAL a, SAN_SIGNAL b, double weightA = 1.0, double weightB = 1.0)

//+------------------------------------------------------------------+
//|This is a generic differentiation formula                                                               |
//+------------------------------------------------------------------+
DTYPE Stats::slopeVal(
   const double &sig[],
   const int SLOPEDENOM = 3,
   const int SLOPEDENOM_WIDE = 5,
   const int shift = 1
)

//+------------------------------------------------------------------+
//| computeStencilKinematics is a superior diff formula that can replace slopeVal  |
//+------------------------------------------------------------------+
DTYPE Stats::computeStencilKinematics(const double& sig[], int shift = 3)




fastSIG = (fuseSIG(fuseSIG(ss.fsig5, ss.fsig14), ss.fsig30) != SAN_SIGNAL::NOSIG) ? fuseSIG(fuseSIG(ss.fsig5, ss.fsig14), ss.fsig30) : SAN_SIGNAL::CLOSE;


SAN_SIGNAL SanSignals::fastSlowSIG(

   const double fastSig,
   const double slowSig,
   const double thresholdPct=0.0005 // e.g., 0.0005 for 0.05% separation
)

 fsig5 = sig.fastSlowSIG(indData.close[SHIFT], indData.ima5[SHIFT],0.0005);
   fsig14 = sig.fastSlowSIG(indData.close[SHIFT], indData.ima14[SHIFT],0.0005);
   fsig30 = sig.fastSlowSIG(indData.close[SHIFT], indData.ima30[SHIFT],0.0005);
   fsig60 = sig.fastSlowSIG(indData.close[SHIFT], indData.ima60[SHIFT],0.0005);
   fsig120 = sig.fastSlowSIG(indData.close[SHIFT], indData.ima120[SHIFT],0.0005);
   fsig240 = sig.fastSlowSIG(indData.close[SHIFT], indData.ima240[SHIFT],0.0005);
   fsig500 = sig.fastSlowSIG(indData.close[SHIFT], indData.ima500[SHIFT],0.0005);


   baseSlopeSIG = sig.kineticAccelerationSIG(baseSlopeData.val1,baseSlopeData.val2,0.015,-0.06," Base");
   slope30SIG = sig.kineticAccelerationSIG(imaSlope30Data.val1,imaSlope30Data.val2,0.015,-0.2," Slope30");


TASK 2:

Task 1 is complete and now to move on to task 2. The app needs an order execution framework to place orders based on buy and sell.
Place just one order on buy/sell signal.
Close trade on close signal or an opposite to current trade signal
If there is an existing trade in place do not place another order
Do not place stop loss or take profit limits.
Take cues from code_references/mq4/code_base_v98.txt and code_references/cBot/PhyBot.cs

These contain a strategy that places one trade on each new bar as long as the signal holds for upto 15 trades at one time
We will not do that and place only a single trade at a time

TASK 3:

Code Refactor: The overall objective and though process:

The application is to be refactored and split into two layers. the functionality in csys is system later that performs low level functions
such as connect to ctrader open api and fetch data and push back data in a standard and consistent manner. So all proto requests

The layer capp should hold all the business logic and will keep getting updated as time goes and new feature are added.

refactor the code as below
ctrader_api, ctypes and cindicators belongs to -> csys
csignals and cstrategies belongs to ->capp
mv ctraderpp.py to -> capp


TASK 4:


Issues & Recommendations
Here are the main points that need attention:
1. Duplicate ctypes.py (Minor but Confusing)
You now have ctypes.py in multiple places:

csys/ctypes.py
csys/ctrader_api/ctypes.py
capp/csignals/types.py

Recommendation:

Keep the canonical definition in csys.ctrader_api.ctypes.
Make csys/ctypes.py a pure re-export (facade), which you’ve already started doing.
Consider removing capp/csignals/types.py and importing DTYPE, T_SIG, SIG from csys.ctypes instead (to avoid duplication).

2. Import Organization in capp/ctraderapp.py
The imports are a bit mixed:

from csys.ctrader_api import CTraderOpenAPI, OrderExecutor
from csys.ctypes import ...
from csys.cindicators.snapshot import compute_indicators

from capp.csignals import SanSignals
from capp.cstrategies import CStrategies

Consider ->

# System layer
from csys.ctrader_api import CTraderOpenAPI, OrderExecutor
from csys.ctypes import ...
from csys.cindicators.snapshot import compute_indicators

# Application layer
from capp.csignals import SanSignals
from capp.cstrategies import CStrategies


3. Inconsistent Package Naming
You have:

capp/csignals/
capp/cstrategies/

While the main modules are:

capp/ctraderapp.py

Recommendation: Consider renaming for consistency:

capp/signals/ instead of capp/csignals/
capp/strategies/ instead of capp/cstrategies/

The c prefix feels unnecessary now that they’re already inside capp/.


4. Minor Code Observations

In ctraderapp.py, the run_signal_cycle method is quite long. Consider extracting the position state update logic into a small helper method.


TASK 5:

Techincally this task is part of /plan command.
Do nothing other than learn and form a base objective

Dive deep into codebase of code_references/mq4 and code_references/cBot
Absorb core design idea and objectives especially from code_references/mq4.
The code was migrated from  code_references/mq4 to code_references/cBot
code_references/mq4 contains a lot of bloat. however we need only a strategies encompassed in HSIG::iniSIG and the one or two baseSlopeSIG and slope30SIG, fsig and fastSIG
based strategies from here. The rest of the flows may be ignored

TASK 6:

As an learning extension of TASK 5: Pay close attention to class MarketMetrics in code_references/mq4/code_base_v98.txt. This class encopasses units and measures and
building blocks for signals and signal generation. Read and learn this class for now.
Do nothing

TASK 7:

total_trade_profits = The current total trade profits currently held.
For eg if 3 trades of usdjpy and 4 trades of eurusd are being held then the total avg trade profit is profits of ((3 usdjpy + 4 eurusd separate)/7).
The strategy checks if the avg trade profit of all assets is above a specific threshold then it closes the trade to capture profits

You will find something like so from code_references/mq4/code_base_v98.txt

//+------------------------------------------------------------------+
//| Calculates the total floating PnL for the current symbol         |
//+------------------------------------------------------------------+
double SanUtils::getTotalTradeProfits(const ulong magicNumber) {
   int totalOrders = OrdersTotal();
   double totalProfit = 0.0;

// Loop backwards to prevent skipped indices
   for(int i = totalOrders - 1; i >= 0; i--) {
      if(
         OrderSelect(i, SELECT_BY_POS, MODE_TRADES)
         &&(OrderMagicNumber() == magicNumber)
      ) {

         // Filter by symbol (ignoring magic number for now as requested)
         if(OrderSymbol() == _Symbol) {

            // Only count active Buy and Sell positions (ignore pending orders)
            if(OrderType() == OP_BUY || OrderType() == OP_SELL) {

               // Sum up net profit: Gross Profit + Swaps + Commissions
               totalProfit += (OrderProfit() + OrderSwap() + OrderCommission());
            }
         }
      }
   }

//return NormalizeDouble(totalProfit,2);
   return (totalOrders>0)?(totalProfit/(double)totalOrders):0.0;
}


This should belong to a SanUtils class in capp/utils.py.

Create the class and function but do not wire it in for now.

TASK 8:

Inside capp/utils.py Keep the magic number optional. magic number is an mq4 concept and does not exist in cTrader.
ensure

TASK 9:

Following is an example of IndData getting initialized in PhyBot.cs.
For full reference code_references/cBot/PhyBot.cs

Points to note, all the values off the Symbol are picked and populated into inddata for the specific symbol id.
Values such as pip_value, pip_size, digits, TickSize.
Please ensure the same is getting populated inside CTraderApp class as well in init_ind_data() function
Derive proper equivalent values of PipValue, PipSize and TickSize and populate indata in CTraderApp
_Period is a concept in Mq4 and should contain the number of minutes present in the current time frame of the assets for eg for M1 _Period ought to be 60 seconds





        public void InitIndData()
        {


            _indData = new IndData

            {
                MagicNumber = 1002,
                BarsHeld = 0,
                Open = GetHistory(idx => Bars.OpenPrices[idx], 500),
                High = GetHistory(idx => Bars.HighPrices[idx], 120),
                Low = GetHistory(idx => Bars.LowPrices[idx], 120),
                Close = GetHistory(idx => Bars.ClosePrices[idx], 500),
                Time = GetHistory(idx => Bars.OpenTimes[idx].ToOADate(), 500).Select(t => DateTime.FromOADate(t)).ToArray(),
                TickVolume = GetHistory(idx => Bars.TickVolumes[idx], 500),
                StdClose = GetHistory(idx => _stdClose.Result[idx], 500),
                StdOpen = GetHistory(idx => _stdOpen.Result[idx], 500),
                Mfi = GetHistory(idx => _mfi.Result[idx], 500),
                Atr = GetHistory(idx => _atr.Result[idx], 500),
                Adx = GetHistory(idx => _adx.ADXR[idx], 500),     // main line = ADXR (smoothed ADX)
                AdxPlus = GetHistory(idx => _adx.DIPlus[idx], 500),   // +DI
                AdxMinus = GetHistory(idx => _adx.DIMinus[idx], 500),
                Obv = GetHistory(idx => _obv.Result[idx], 500),
                Rsi = GetHistory(idx => _rsi.Result[idx], 500), // -DI
                Ima5 = GetHistory(idx => _ima5.Result[idx], 500),
                Ima14 = GetHistory(idx => _ima14.Result[idx], 500),
                Ima30 = GetHistory(idx => _ima30.Result[idx], 500),
                Ima60 = GetHistory(idx => _ima60.Result[idx], 500),
                Ima120 = GetHistory(idx => _ima120.Result[idx], 500),
                Ima240 = GetHistory(idx => _ima240.Result[idx], 500),
                Ima500 = GetHistory(idx => _ima500.Result[idx], 500),
                AvgStd = GetHistory(idx => _avgStdDev.Result[idx], 500),
                Point = Symbol.TickSize,
                Digits = Symbol.Digits,
                PipValue = Symbol.PipValue,
                PipSize = Symbol.PipSize,
                DBL_EPSILON = (Symbol.TickSize * 0.1),
                // cTrader .NET 6 standard way to get total minutes
                _Period = GetMinutes(TimeFrame),
                Current_Period = GetMinutes(TimeFrame),
                Shift = 0,
                TotalOrders = Positions.Count(p => p.Label == _label && p.SymbolName == SymbolName)
            };
        }


TASK 10 : depends on TASK 7:

Let us wire in the SanUtils::get_total_trade_profits in capp/utils.py into CTraderApp. In order to calculate total trade profits
CTraderApp needs to make calls to get a list or a sequence of postions being held.
Feed the sequence of positions into

First think about retrieving the sequence of positions. Detail out which calls will be made to client.py for this. Does it make sense for this
positional seq call to be part of utils.py or CTraderApp?
















